const famous_person: string = "Elon Musk";
const quote: string = "When something is important enough, you do it even if the odds are not in your favor.";
const message: string = `${famous_person} once said, "${quote}"`;

console.log(message);
