// Create an object to store information about car
let car: { make: string; model: string; year: number } = {
    make: "Honda",
    model: "Civic",
    year: 2019
};

// Print the object
console.log(`Car Info: Made by ${car.make}, model ${car.model}, year ${car.year}`);

